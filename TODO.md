# MyHomeLib — TODO

| # | Task | Size | Status |
|---|------|------|--------|
| 1 | MyHomeLib MCP server (`Utils/MHLMcpServer`) — 9 tools (6 catalogue + `get_book_toc`/`get_book_text`/`search_in_book`), all automated tests pass (45 protocol + 10 extractor + 43 cache). Manually verified against the running app and confirmed working 2026-07-26. Shipped to users in 2.6.1: the installer now packages `MHLMcpServer.exe` next to `MyHomeLib.exe`, and the help documents it in «MCP-сервер для AI-асистентів» | 🟠 L | ✅ Done |
| 2 | Add a `--make-fixture` CLI mode to `MHLMcpServer.exe` that builds a tiny SQLite collection via `TBookCollection_SQLite.CreateTemp`, so the 6 catalogue tools (currently manual-checklist-only) get automated coverage in `tests/run_tests.js`. **Now the blocking fix, not just a nice-to-have:** the 33 DB-backed cases are pinned to `collection_id: 1` and are *directory*-dependent, not merely machine-dependent — MyHomeLib prefers a portable `myhomelib2.ini` beside the exe over `%APPDATA%`, so the same binary sees a different library depending on where it was built. Run from `Program/OUT/Bin64` in the main checkout (which has such an ini) the collections are ids 3 and 4, and all 33 fail with `collection_not_found`; run from a checkout without one they use `%APPDATA%`, where collection 1 exists, and all 33 pass. A self-built fixture collection removes the dependency entirely | 🟡 M | 🔴 Open |
| 3 | Escape the seven spliced `search_books` arguments (`title`, `author`, `series`, `lang`, `keyword`, `annotation`, `min_lib_rate`) the same way the `genre` field was fixed — see the "It is not free of it elsewhere" note in `Utils/MHLMcpServer/README.md`. Implemented: `LiteralLikeCondition` renders each value as chunked SQLite `CHAR(...)` code points instead of a quoted literal, so no caller character reaches the SQL text; `min_lib_rate` now emits `>= N` instead of `="N"`. 34 automated cases (28–34 new) pass on both Win64/Win32; ground-truth-verified against the real collection (see `.superpowers/sdd/2026-07-25-mhl-mcp-server/task-11-report.md`). Awaiting manual confirmation in the running app before close-out | 🟡 M | 🔴 Open |
| 5 | Decide how an explicit JSON `null` argument should behave, then make the three helpers agree. Today `ArgStr` treats `null` as absent (silently no filter) while `ArgInt`/`ArgBool`/`RequireInt` reject it with `invalid_params`. Both readings are defensible — treating `null` as "not supplied" is a common JSON convention, under which the integer/boolean helpers are the deviant ones — so this is a convention call, not a bug. Found by the final whole-branch re-review; parked deliberately rather than settled in an unreviewed fix. `unit_MCP_Json.pas:50-119` | 🟢 S | 🔴 Open |
| 6 | Multilingual UI (planned start: week of 2026-07-27). Codebase is already well-prepared: **1034** `resourcestring` entries compiled into the exe (counted from `Program/OUT/Bin64/MyHomeLib.drc`), **~790** localizable DFM properties (645 `Caption`, 80 `Hint`, 65 `Text`) across 47 forms/frames, and only **~60** stray Cyrillic literals still inline in `.pas` (plus 49 in the separate `MHLMcpServer`). Chosen approach: assign `System.LoadResStringFunc` (`System.pas:2875`, an official assignable RTL hook) at startup to redirect all 1034 code strings — and the VCL's own strings — through a JSON catalog per locale in `Lang\`, plus a runtime component-tree walker for DFM `Caption`/`Hint`/`Text`, `TActionList` items and VirtualTree headers, plus a Node extractor script to keep catalogs in sync with source. Explicitly **not** TsiLang (it rewrites DFMs — see the `frm_main.dfm` re-save damage history) and **not** dxgettext (msgids would be Ukrainian sentences). Replace the dead `TAppLanguage = (alEng, alRus)` at `unit_Globals.pas:92` / `unit_Settings.pas:713` with a locale string. Any stale `Interface/Lang` integer already sitting in a user's `myhomelib2.ini` is **ignored, not migrated** (settled 2026-07-26) — the value was never read, so it carries no real preference; fall back to the default locale when it doesn't parse. **Out of scope — settled 2026-07-26:** genre names are collection properties populated at collection creation and live inside each collection's SQLite DB (plus `Installer/Common/*.glst`). They are library data, not UI chrome, and stay in the collection's own language — no genre-code → localized-name lookup, no `.glst` translation. Remaining scope: 54 Ukrainian HTML help pages in `Program/Help/` per language, and Inno Setup installer strings. Estimate: ~1.5–2 weeks infrastructure, ~1 week per additional language (mostly translation review, not engineering) | 🔴 XL | 🔴 Open |
| 4 | `search_books` with every criteria field left blank fails with `"Перевірте параметри фільтра"` (a raw `-32603`, not a mapped domain error) instead of returning an unfiltered page or a clear `invalid_params` — `TBookCollection_SQLite.PrepareSearchData` rejects an all-blank filter outright. Discovered while testing Task 10's fix rounds; pre-existing DAO behavior, not a regression. Implemented: `search_books` now detects an all-blank filter (every field empty and `include_deleted: true`) itself and raises `EMcpToolError.Create('empty_filter', ...)` — `isError: true` with a machine-readable code, not a raw `-32603`. Case `33` covers it. Awaiting manual confirmation in the running app before close-out | 🟢 S | 🔴 Open |
